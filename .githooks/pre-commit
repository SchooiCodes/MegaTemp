#!/usr/bin/env bash
set -euo pipefail

echo "==> Running ruff check..."
ruff check .

echo "==> Running ruff format check..."
ruff format --check .

echo "==> Checking Python imports..."
python -c "from utilities.exceptions import MegaTempError, RegistrationError, ConfigError, CredentialError, ProxyError, EmailProviderError, BrowserError, APIError, AccountError; print('Custom exceptions OK')"
